import { FilterTodayPipe } from './filter-today.pipe';

describe('FilterTodayPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTodayPipe();
    expect(pipe).toBeTruthy();
  });
});
